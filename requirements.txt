pandas>=2.0
numpy>=1.24
requests>=2.31
pyarrow>=14.0
scikit-learn>=1.3
lightgbm>=4.0
matplotlib>=3.7
folium>=0.15

# Faz 2 — ETAS baseline ve CSEP değerlendirmesi
# ETH Zürich ETAS implementasyonu; PyPI'da yok, git'ten kurulur.
etas @ git+https://github.com/lmizrahi/etas.git
seismostats     # etas.simulation bağımlılığı
pycsep>=0.8     # N/S/T testleri, CSEP uyumlu değerlendirme

# Faz 3+:
# shap          # öznitelik önem analizi
# geopandas     # fay shapefile işleme

# Faz 3 — ML
shap>=0.45      # öznitelik katkı analizi

# Faz 3 — jeofizik katmanlar
cutde           # yarı-uzayda üçgen dislokasyon (Coulomb gerilim değişimi)
pytest>=8.0      # tutarlılık testleri
